services:
  - type: web
    name: finance-manager
    runtime: java
    buildCommand: mvn clean package -DskipTests
    startCommand: java -jar target/finance-manager-0.0.1-SNAPSHOT.jar
    envVars:
      - key: PORT
        value: 8080
